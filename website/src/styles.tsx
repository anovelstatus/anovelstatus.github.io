import type { SxProps, Theme } from "@mui/material";

/** Make the popup cards a little more distinct from the page beneath */
export const popupCardStyles: SxProps<Theme> = {
	maxWidth: 500,
	border: "2px solid #6e6e6eff",
};
